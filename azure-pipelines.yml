trigger: none
pr: none

stages:
- stage: A
  jobs:
  - job: A1
    steps:
    - bash: echo "##vso[task.setvariable variable=TEST;isOutput=true]true"
      name: A1S
- stage: B
  dependsOn: A
  condition: dependencies.A.outputs['A1.A1S.TEST']
  jobs:
    - job: B1
      steps:
      - bash: echo "Hello World"

