trigger: none
pr: none

stages:
- stage: Init
  jobs:
    - job: init
      steps:
        - task: AzureCLI@2
          displayName: Azure CLI
          inputs:
            azureSubscription: gopuff-dev
            scriptType: bash
            scriptLocation: inlineScript
            inlineScript: |
              az network front-door -h